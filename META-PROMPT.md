# go 開発ルール（メタコンテキスト全文・デスクトップGUI=Wails v2 版）

## 最重要（絶対に守る）

- **追加機能禁止**：指示にない機能を勝手に足さない
- **既存動作破壊禁止**：互換性最優先
- **このフォルダ1つで完結**：丸ごと別の場所に移動しても動くこと
- **配布バイナリは必ずバージョン付き**
  - 例: `faxocr-go_v0.0.exe`
  - **0.1ずつ増やす**（`v0.0 → v0.1 → v0.2 ...`）
- **他PCにライブラリが入っていない前提で動くこと**
  - 配布物は「単体バイナリ + 同梱ファイルのみ」
  - 外部DLL/共有ライブラリ依存は禁止
- **エラーは握りつぶさない**
  - `if err != nil` ごとに「何が失敗したか」ログを出す

---

## 言語

Go

---

## バージョンとログ（忘れ防止のため必須）

### 1) exeに埋め込む情報（固定情報）
- バージョン（必須）
- ビルド日時（推奨）
- commit hash（任意）

### 2) 実行時ログ（必須）
- 実行するたびに **exeと同じフォルダ**へログを残す
- ログは **標準出力 + ファイル**の両方に出す（GUIの場合はファイルが主）

ログに必ず含める項目:
- バージョン
- OS/ARCH
- 起動時刻
- 実行引数（GUIの場合は起動オプション）
- config.json の読み込み元（CLI/env/jsonどれか）
- 成功/失敗件数
- エラー詳細（握りつぶさない）

---

## 基本方針

- 外部依存は最小限（ただしGUIはWails v2を使用）
- Windows / Mac / Linux クロスコンパイル対応（OS依存コード禁止）
- コメントは日本語

---

## フォルダ構成


※ Wails v2導入により、従来の `web/` + `net/http` 単独構成は「Web UI」用途としては維持可能だが、
「デスクトップGUI」はWails v2を正式採用する。

---

## CLI

- 引数とオプションは `flag` パッケージ
- ファイル操作系は `--dry-run` を用意
- `--help` で使い方表示

---

## デスクトップGUI（Wails v2）規定（必須）

### 目的
- 配布先のPCに追加インストール無しで動く「デスクトップGUIアプリ」を作る。

### 技術方針（固定）
- デスクトップGUIは **Wails v2** で作成する
- CLIとデスクトップは **内部ロジックを internal/ に共通化**する
- GUI専用ロジックは `cmd/desktop/` と `desktop/` 以下に閉じる

### 配布物の形（必須）例
- 基本は **フォルダ配布**
  - `faxocr-go-desktop_vX.Y/`
    - `faxocr-go-desktop_vX.Y.exe`
    - `README.txt`（最小の使い方）
    - `config.json.example`
    - `logs/`（初回起動で自動作成）

### EXEの命名規則（必須）
- 形式: `faxocr-go-desktop_v{X.Y}.exe`など
- バージョンは **0.1刻み**で増やす
- 同一フォルダに複数バージョンを置いても衝突しないこと

### 実行時の動作規定（必須）
- 作業ディレクトリに依存しないこと
  - `os.Executable()` を基準に「実行ファイルと同じフォルダ」をホームとみなす
- `config.json` の探索は「実行ファイルと同じフォルダ」を含むこと（既存ルールの優先順位は維持）
- ログは必ず「実行ファイルと同じフォルダ配下」に出す
  - 例: `{exe_dir}\logs\YYYYMMDD_HHMMSS.log`
- 例外やパニックは握りつぶさず、ログに出して非0で終了する（可能な範囲で）

### Windows互換性（必須）
- パスは `filepath` を使う（手で `\` を書かない）
- 文字コード前提を置かない（日本語ファイル名を扱えること）
- 管理者権限不要で動くこと（Program Files への書き込み前提禁止）
- 書き込み先は exe 同階層か、ユーザーディレクトリ（必要なら）に限定する

### Wails v2 固有ルール（必須）
- `wails build` の成果物は **単体exeとして動く**こと（追加DLLを要求しない）
- frontendは **オフライン動作**（外部CDN禁止）
- frontendの成果物はビルドに含める（ビルド漏れ禁止）
- Go⇔frontend間のAPIは最小限にし、型とエラーを明確にする

---

## 外部API

- REST直叩き（クライアントライブラリは使わない）
- APIキーの読み込み優先順位:
  1. コマンドライン引数
  2. 環境変数
  3. config.json（実行ファイルと同じフォルダ内）
  4. 見つからなければエラー
- GUIにもAPI設定画面を用意（キー入力・保存・テスト）

---

## ログ出力例



---

## テスト

- `go test ./...` で全テスト実行
- 主要なロジックにはテストを書く
- テストデータは `testdata/` に置く

---

## ビルド

### CLI（開発用）

```bash
go build -o faxocr-go .

# 例: v0.0
GOOS=windows GOARCH=amd64 go build -o faxocr-go_v0.0.exe ./cmd/cli
GOOS=darwin  GOARCH=arm64 go build -o faxocr-go_v0.0-mac ./cmd/cli
GOOS=linux   GOARCH=amd64 go build -o faxocr-go_v0.0-linux ./cmd/cli
# desktop/ 配下で実行する想定
wails build
