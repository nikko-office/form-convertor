---
tags:
  - 要件定義
  - MasterPieceMini
  - 帳票生成
  - Playwright
  - PDF生成
  - SVG
  - Mac-mini
  - システム設計
created: 2026-02-13
updated: 2026-02-13
aliases:
  - MasterPiece Mini
  - 帳票生成システム
---

## 帳票生成システム「MasterPiece Mini」最終確定 要件定義書

> Mac mini 常駐サーバー上で稼働し、Playwright による高精度レンダリングで帳票PDFを生成するシステム。100%ベクター品質、TSV直接採用、画像コピペ、プレビュー上での直接編集を実現する。

---

## システム基盤

- **稼働環境：** Mac mini（24時間常駐サーバー）
- **生成エンジン：** Playwright（headless Chromium）による高精度レンダリング
- **描画品質：** 100%ベクター（SVG）および CSS によるミリ単位制御
- **保存先：** Mac mini 内ローカルディレクトリ（`/output`）

---

## 書類の向き・サイズ規定

帳票種別ごとに、以下の物理設定をプログラム側で自動制御する。

- **縦向き（Portrait）：** A4 / A5 選択可能
- **横向き（Landscape）：** A4 / A5 選択可能
- **共通：** CSS の `@page` プロパティを用いて、出力時に余白ゼロまたは指定ミリ数の精度で固定する

---

## 対象帳票（テンプレート種別）

以下の帳票ごとに、固有のHTML構造とロジックを保持する。

- 注文請書
- 切断証明書
- 見積書
- 注文書
- 荷渡依頼書
- お知らせ
- 請求書
- その他（フリーレイアウト・画像メイン帳票）

---

## 入力データ・コンテンツ編集要件

### TSV（タブ区切りデータ）の直接採用

- Excel 等のセル範囲をコピー＆ペーストし、そのまま帳票内のテーブル（表）部分に表示
- 流し込んだ後のテーブル内の数値や文字は、ブラウザ上で直接編集（書き換え）が可能

### 画像のコピペ添付

- クリップボードから画像を直接ペースト
- Base64 埋め込みによりサーバーパスに依存せず保持
- 画像主体（お気軽帳票）の際は、印字エリアに合わせた自動スケーリングを適用

### テキスト直接編集

- 宛名、但書き、備考欄など、全てのテキスト項目はプレビュー画面上で直接編集可能

---

## スタンプ・ロゴ管理（一貫性要件）

### 完全SVG化

- **社内ハンコ：** SVG 形式で1箇所（`/assets/svg/`）に集約。拡大しても劣化しない品質を維持
- **ロゴ：** インライン SVG として埋め込み。PNG / JPG 等のビットマップ画像を一切排除

### 配置ルール

- 帳票の向き（縦横）に関わらず、指定された座標に最前面でオーバーレイ（重畳）表示
- CSS の乗算（`multiply`）処理により、下地の文字を透かすリアルな印影を再現

---

## レイアウト構造要件

- **基本比率：** 1対2、あるいは2対1の分割レイアウトを基本構造として管理
- **画像メイン時：** 定型テンプレートを介さず、画像を有効印字エリアに最大化表示し、ハンコのみを合成

---

## 運用ディレクトリ構造

```text
/report-system
├── /assets
│   ├── /svg            # ハンコ(hanko.svg), ロゴ(logo.svg) ※全帳票共通
│   └── /css            # 共通スタイル(A4/A5、縦/横の定義)
├── /src
│   ├── /templates      # 請求書.html, 荷渡依頼書.html 等（全8種以上）
│   └── /core           # TSVパース、画像Base64変換、PDF生成ロジック
└── /output             # 生成PDF保存フォルダ
```

---

## 関連ページ

- [[META-PROMPT]]
